<footer class="w-full py-4 mt-6 text-white bg-blue-600">
  <div class="container mx-auto text-center">
    <p class="text-sm">
      &copy; <%= Time.now.strftime("%Y") %> <%= metadata.title %>
      | Página compilada em <%= Time.now.strftime("%H:%M %d/%m/%Y") %> (<a href="<%= metadata.github_url %>" rel="noopener noreferrer" target="_blank" class="text-white">Código fonte</a>)
    </p>
    <p class="text-sm">
      Todos os direitos reservados.
      Este site tem apenas propósito educacional e comparativo e não possui qualquer vínculo com empresas supracitadas.
      Informações podem estar incorretas ou desatualizadas.
    </p>
  </div>
</footer>
